apiVersion: v1
kind: Service
metadata:
  name: lwc-web
  labels:
    app: lwc-web
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: lwc-web
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
